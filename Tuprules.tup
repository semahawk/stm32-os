.gitignore

CROSS_COMPILE = arm-none-eabi-

CC = $(CROSS_COMPILE)gcc
CFLAGS = -mthumb -mcpu=cortex-m3 -static -O0 -Wall -Wextra -Werror

LD = $(CROSS_COMPILE)ld

!cc = |> $(CC) $(CFLAGS) -c %f -o %o |> %B.o
!ld = |> $(LD) $(LDFLAGS) -o %o %f |>
